{% extends 'base.html.twig' %}

{% block title %}Edit Purchase{% endblock %}

{% block body %}
  <h1>Edit Purchase</h1>

  {{ form_start(form) }}
      {{ form_widget(form) }}
      <button class="btn btn-primary">Save</button>
  {{ form_end(form) }}

  <a href="{{ path('app_purchase') }}">Back to list</a>
{% endblock %}
